import React from 'react';
// 编辑组件

class DeviceEdit extends React.Component {
  render() {
    // const {device} = this.state;
    console.log('device');
    // console.log(device);
    // return device ? <DeviceEditor editTarget={device}/> : <span>加载中...</span>;
    return <span>加载中...</span>;
  }
};

export default DeviceEdit;
